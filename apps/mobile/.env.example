# Copy to .env. Both are safe in the app bundle: the anon key only ever grants
# what RLS allows.
#
# These point at the DEV project, so `npm run mobile` never touches the real
# ledger. The app derives its identity from this URL: pointed here it builds as
# "Batwa Dev" (package online.batwa.app.dev) and shows the DEV banner. Only the
# production EAS profile supplies the prod project, and only that build is the
# real app.
EXPO_PUBLIC_SUPABASE_URL=https://edaxpqeszssgmlfpsyrn.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_fvsrx9tpEEOsaiNRKV2kTg_gO2CrhqR
